language: erlang
sudo: required
dist: trusty

group: edge  # Add this

env:
  - LUA="luajit"

branches:
  only:
    - dev

install:
  - cd GUIDING_PRINCIPLES/ && bash aio.sh

script:
  - source setup.sh && /usr/local/bin/busted --lua=luajit RUNTIME/COLUMN/code/test/exp/test_vector.lua

notifications:
  email:
    recipients:
      - isingh@nerdwallet.com
    on_success: change
    on_failure: always
