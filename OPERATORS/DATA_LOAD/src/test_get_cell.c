#include <stdio.h>
#include <string.h>
#include "q_macros.h"
#include "q_types.h"
#include "get_cell.h"

int
main(
    int argc,
    char **argv
    )
{
  int status = 0;
  char *X = "abc,def,123,456\n"; size_t nX = strlen(X);
  size_t xidx = 0;
  int bufsz = 32;
  char buf[bufssz]; 
  for ( int i = 0; i < 4; i++ ) {
  }
BYE:
  return status;
}
