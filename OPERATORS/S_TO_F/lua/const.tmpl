return require 'code_gen' {

   declaration = [[

#include <stdint.h>
extern int
const_${out_q_type}(
  ${out_c_type} *X,
  uint64_t nX,
  ${out_c_type} *ptr_val
  );
   ]],
   definition = [[

#include "_const_${out_q_type}.h"


//START_FUNC_DECL
int
const_${out_q_type}(
  ${out_c_type} *X,
  uint64_t nX,
  ${out_c_type} *ptr_val
  )
//STOP_FUNC_DECL
{
  int status = 0;
  ${out_c_type} val = *ptr_val;
// TODO: Why seg fault when following uncommented?
// #pragma omp parallel for
  for ( uint64_t i = 0; i < nX; i++ ) { 
    X[i] = val;
  }
  return status;
}


   ]]
}
